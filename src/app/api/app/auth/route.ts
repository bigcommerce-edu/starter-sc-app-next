export function GET(): void {
  // TODO: Implement the install callback
  //  - Read code/context/scope from the query string
  //  - Call installStore, mapping its errors to the right /app-error reason
  //  - Redirect to this app's home page for the new store on success
  //  - No registerAppExtension call yet - that's the graphql-ext enhancement
}
