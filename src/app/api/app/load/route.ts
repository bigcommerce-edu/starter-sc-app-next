export function GET(): void {
  // TODO: Implement the launch callback
  //  - Read signed_payload_jwt from the query string
  //  - Call loadStore, mapping its errors to the right /app-error reason
  //  - Redirect to the deep link loadStore returns
}
