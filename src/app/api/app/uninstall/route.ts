export function GET(): void {
  // TODO: Implement the uninstall callback
  //  - Server-to-server, not browser-facing - returns JSON, not a redirect
  //  - Read signed_payload_jwt, call uninstallStore, 401 on a bad signature
}
