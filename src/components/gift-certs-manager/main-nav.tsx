// TODO: Implement MainNav
//  - Primary navigation for the gift certificates manager
//  - Links to Gift Certificates, Customers
//  - Highlight the item matching the current section, read out of the
//    pathname
//  - The index route has no section segment in its path, so treat it as the
//    section the root path re-exports rather than leaving nothing
//    highlighted
//  - Style the active item's pill with an inline `style` rather than Box's
//    backgroundColor/padding props, taking the values from the theme - a
//    styled-components generated class can be dropped in the control panel
//    iframe, where the prerender and runtime registries disagree
