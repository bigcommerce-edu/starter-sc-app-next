export function AuthorizedPage(): null {
  // TODO: Implement AuthorizedPage
  //  - Read storeHash from params, call isAuthorizedForStore
  //  - redirect("/unauthorized") on failure; otherwise render pageComponent
  //  - Wire this into every store-scoped page.tsx, which don't use it yet
  return null;
}
