export function exchangeCodeForToken(): void {
  // TODO: Implement exchangeCodeForToken
  //  - POST to BigCommerce's OAuth token endpoint with client id/secret,
  //    code, context, scope, and redirect_uri
  //  - Parse and validate the response; throw TokenExchangeFailedError on
  //    any failure (network, non-2xx, or unexpected shape)
}
