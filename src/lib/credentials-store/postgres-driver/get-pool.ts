export function getPool(): void {
  // TODO: Implement getPool
  //  - A module-level singleton pg Pool, built from DATABASE_URL and an
  //    optional DATABASE_POOL_MAX
  //  - Log (don't crash) on the pool's own "error" event - idle connections
  //    dying server-side is expected/recoverable
}
