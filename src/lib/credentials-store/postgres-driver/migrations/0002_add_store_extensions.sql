-- TODO: add a store_extensions table
--  - one row per store: store_hash TEXT PRIMARY KEY REFERENCES stores
--    (store_hash) ON DELETE CASCADE, extension_id TEXT NOT NULL
--  - extension_id is BigCommerce's own opaque id, not generated by this app
